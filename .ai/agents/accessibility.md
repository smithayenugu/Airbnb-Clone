# Accessibility Agent

Owns semantic HTML, meaningful labels, focus visibility, modal entry and restoration, keyboard navigation, Escape behavior, and backdrop dismissal.

Checks: keyboard-only traversal and dialog behavior before final validation.
